/** @format */

const Notifications = ({ value }) => {
  return (
    <div className="row my-5 d-flex justify-content-center align-items-center">
      <div className="col-auto px-3 btn btn-danger">{value}</div>
    </div>
  );
};

export default Notifications;
